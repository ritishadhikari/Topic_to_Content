# CI Pipeline
name: Backend CI Pipeline

on: 
  push:
    branches:
      - main
  pull_request:
    branches: 
      - main

jobs:

  test:
    
    runs-on: ubuntu-latest

    # Spin up a live mongodb container for our hybrid mocks to query
    services:
      mongodb:
        image: mongo:latest
        ports:
          - 27017:27017
        # Mirroring the development environment
        options: >-
          --health-cmd mongostat
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        command: ["--replSet", "rs0", "--bind_ip_all", "--port", "27017"]
    
    # Provide dummy environment variables for the test suite
    env:
      MONGO_URI: mongodb://localhost:27017
      JWT_SECRET_KEY: test-secret-key-123
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 120
      MCP_USER: default_test_user
      OPENAI_API_KEY: sk-dummy-key
      TAVILY_API_KEY: tvly-dummy-key

    steps:

      - name: Checkout Code Repository
        uses: actions/checkout@v4


      - name: Setup Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'
      
      - name: Initialize MongoDB Replica set
        run: |
          docker exec $(docker ps -q -f ancestor=mongo:latest) mongosh --eval "rs.initiate({_id:'rs0', members:[{_id:0, host:'localhost:27017'}]})"
          sleep 5
      - name: Install Application Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run Pytest
        env:
          MONGO_URI: "mongodb://localhost:27017/?replicaSet=rs0"
        run: |
          pytest -v
  
  build-and-push:
    needs: test     # waits for the test to pass
    runs-on: ubuntu-latest

    # Only Push images when code is merged directly into the main branch
    if: github.ref == 'refs/heads/main' && github.event_name== 'push'

    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Set lowercase repository name
        run: |
          echo "REPO=${GITHUB_REPOSITORY,,}" >> ${GITHUB_ENV}

      - name: Login to the Github container registry
        uses: docker/login-action@v3
        with: 
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ env.REPO }}
      
      # Build and Push the FastAPI backend
      - name: Build and push FastAPI Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ghcr.io/${{ env.REPO }}-api:latest
          labels: ${{ steps.meta.outputs.labels }}
      
      # Build and Push the FastMCP Server
      - name: Build and Push FastMCP Server
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.mcp
          push: true
          tags: ghcr.io/${{ env.REPO }}-mcp:latest
          labels: ${{ steps.meta.outputs.labels }}
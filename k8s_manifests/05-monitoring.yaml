apiVersion: v1
kind: Namespace
metadata: 
  name: monitoring

---

apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: khudse-backend-monitor
  namespace: kube-system
spec:
  repo: https://prometheus-community.github.io/helm-charts
  chart: kube-prometheus-stack
  targetNamespace: monitoring  # install the promethues in the monitoring namespace and look for service monitor files that shares its release name - khudse-backend-monitor. Once it finds it, it shall start scraping the labels for the services in the namespace (default in this case) instructed to be scanned
  valuesContent: |-

    alertmanager:
      enabled: true
      alertmanagerSpec:
        secrets:
          - alertmanager-credentials
      config:
        global:
          resolve_timeout: 5m
          smtp_smarthost: 'smtp.gmail.com:587'
          smtp_from: 'ritishadhikari@gmail.com'
          smtp_auth_username: 'ritishadhikari@gmail.com'
          smtp_auth_password_file: '/etc/alertmanager/secrets/alertmanager-credentials/smtp_auth_password'
        route:
          group_by: ['alertname', 'job']
          group_wait: 30s
          group_interval: 5m  # will send new failures only after 5 minutes
          repeat_interval: 4h
          receiver: 'email-alerts'
        receivers:
        - name: 'null'
        - name: 'email-alerts'
          email_configs:
          - to: 'adhikari_ritish@rediffmail.com'
            send_resolved: true
          - to: 'radhik0607@gmail.com'
            send_resolved: true   
    grafana:
      enabled: true
      admin:
        existingSecret: "grafana-admin-credentials"
        userKey: admin-user
        passwordKey: admin-password

    prometheus:
      prometheusSpec:
        # keep data for only 2 days time period
        retention: 2d
        resources:
          requests:
            memory: 512Mi
            cpu: 250m
